# Corrigé 7 — IaC, CaC, Pipeline as Code

| Élément | Concept |
|---|---|
| `azure-pipelines.yml` | Pipeline as Code |
| `main.bicep` | Infrastructure as Code |
| `docker-compose.yml` | Configuration as Code |
| Variable Group `vg-croissant-prod` | Configuration as Code / gestion centralisée de configuration |
| App Service `croissant-api-prod` décrit dans Bicep | Infrastructure as Code |
| Step `Docker@2 buildAndPush` | Pipeline as Code |
| `ENVIRONMENT_NAME=staging` | Configuration as Code |
