# Exercice 7 — IaC, CaC, Pipeline as Code

Associer chaque fichier ou élément au bon concept.

| Élément | Concept |
|---|---|
| `azure-pipelines.yml` | ? |
| `main.bicep` | ? |
| `docker-compose.yml` | ? |
| Variable Group `vg-croissant-prod` | ? |
| App Service `croissant-api-prod` décrit dans Bicep | ? |
| Step `Docker@2 buildAndPush` | ? |
| `ENVIRONMENT_NAME=staging` | ? |
